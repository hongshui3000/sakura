# Kconfig - TM4C12X SDK HAL configuration

config HAS_TM4C12X_SDK
	bool

menuconfig HAS_TM4C12X_SDK
	bool "TI TM4C12X SDK support"
	default n
	depends on HAS_CMSIS
	help
	Build required peripheral driverlib files from the TM4C12X SDK
